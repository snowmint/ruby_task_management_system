language: ruby
rvm:
- 3.0.1
before_install:
- gem install bundler
install:
- gem install rails
script:
- bundle install --jobs=3 --retry=3
- bundle exec rails db:reset db:setup db:migrate RAILS_ENV=test
- bundle exec rspec